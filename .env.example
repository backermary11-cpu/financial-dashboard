# Copy to .env.local for local development, and add both to Vercel/Netlify
# (Project → Settings → Environment Variables). Leave unset to run without sync.
# Find them in Supabase → Project Settings → API.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
